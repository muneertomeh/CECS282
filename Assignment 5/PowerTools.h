#ifndef _POWERTOOLS
#define _POWERTOOLS

#include "BigInt.h";

int fibo(int);
int fact(int);
int exp(int, int);
 BigInt fibo(BigInt);
 BigInt fact(BigInt);
 BigInt exp(BigInt, int);
 BigInt exp(BigInt, BigInt);


#endif

